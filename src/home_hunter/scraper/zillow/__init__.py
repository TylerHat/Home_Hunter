"""Legacy Zillow scraper (curl_cffi + Playwright).

Kept for reference from the project's for-sale era. It is **not** wired into the
default NYC-rental pipeline (which uses Craigslist). Reviving it requires the
optional ``curl_cffi`` / ``playwright`` dependencies and a Zillow-shaped Config.
"""
